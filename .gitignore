# Runtime / temp / generated only.
# NOTE: engagement data (authorizations/, projects.json, projects/, logs/) IS
# tracked in this repo by the owner's explicit choice — this is a PRIVATE repo
# used as a self-contained backup. Keep it private.
*.tmp
node_modules/
.playwright-mcp/
.DS_Store
Thumbs.db
