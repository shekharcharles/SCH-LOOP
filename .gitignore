# SCH Loop — the repo is TOOLING ONLY. Client/engagement data + local rules +
# secrets stay on your machine and are NEVER committed. Safe to share / install.

# --- engagement + client data (local only) ---
authorizations/
projects/
projects.json
state.json
**/state.json
logs/
reports/
**/reports/
evidence/
**/evidence/

# --- per-project rules + local config (Claude auto-reads these; never upload) ---
CLAUDE.md
**/CLAUDE.md
.claude/settings.local.json
**/.claude/settings.local.json
# (docs/CLAUDE.template.md and docs/settings.template.json are templates — tracked.)

# --- secrets / credentials (belt-and-suspenders; secret-scan also blocks these) ---
.env
.env.*
*.pem
*.key
*.p12
*.pfx
id_rsa
id_dsa
id_ed25519
credentials.json
service-account*.json
.npmrc
.git-credentials

# --- runtime / generated ---
# the discovered skill registry: machine-specific paths, regenerated by
# `state.mjs skill-discover`. Trust decisions live here too and stay local.
skills.json
*.tmp
node_modules/
.playwright-mcp/
*.png
.DS_Store
Thumbs.db
.sessions.json
.notify-url

# runtime artifacts of the atomic-write path (.bak is a copy of a gitignored file)
*.bak
*.tmp
*.corrupt-*

# full-test-suite lease (runtime, machine-local)
.locks/
external-skills/
