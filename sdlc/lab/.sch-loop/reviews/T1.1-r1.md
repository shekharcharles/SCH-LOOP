# Review T1.1 — round 1

**Verdict:** APPROVE  ·  **Spec:** PASS  ·  raw 0 → unique 0, blocking 0, refuted 0

## Spec checks
- PASS add('x') yields priority normal — src/todo.mjs:10 `add(title, priority = "normal")` and :13 spreads `priority` into the item. Test `add defaults priority to normal` asserts `s.add("x").priority === "normal"`.
- PASS add('x','high') yields priority high — src/todo.mjs:4 `PRIORITIES = ["low","normal","high"]` passes validation; :13 stores it. Test `add accepts an explicit priority` asserts both "high" and "low".
- PASS add('x','urgent') throws /priority/ — src/todo.mjs:12 `if (!PRIORITIES.includes(priority)) throw new Error(`priority must be one of ${PRIORITIES.join("|")}`)` — message begins with the literal word `priority`, so /priority/ matches. Test `add rejects an unknown priority` covers it.
- PASS complete() preserves the priority — src/todo.mjs:23 `items.set(id, { ...item, done: true })` clones the whole item, so `priority` survives; :18 `list()` likewise clones. Test `list and complete carry the priority through` asserts both return "high".
- PASS MUST NOT: no existing test deleted or weakened — Diff of tests/todo.test.mjs shows zero deletions of tests. The one changed line (tests/todo.test.mjs:8) tightens the existing deepEqual from `{id,title,done}` to `{id,title,done,priority:"normal"}` — a strictly stronger assertion under `assert.deepEqual`. The other three existing tests are untouched; four new tests appended.
- PASS Interface: add(title, priority?): {id,title,done,priority} — src/todo.mjs:10-15 returns `{ ...item }` where item is `{ id, title, done, priority }`; priority is a defaulted second parameter, so all existing single-arg call sites keep working.

## Blocking
- none

## Advisory
- none
