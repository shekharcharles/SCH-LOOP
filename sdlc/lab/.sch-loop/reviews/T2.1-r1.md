# Review T2.1 — round 1

**Verdict:** APPROVE  ·  **Spec:** PASS  ·  raw 1 → unique 1, blocking 0, refuted 0

## Spec checks
- PASS 1. list({priority:'high'}) returns only the high-priority items — src/todo.mjs:22 `.filter(i => priority === undefined || i.priority === priority)`; test `list filters by priority` asserts high -> [hi, hi2], low -> ['b'], normal -> [].
- PASS 2. list() with no argument still returns every item, as copies — src/todo.mjs:17 default `= {}` makes priority undefined; filter passes all; `.map(i => ({ ...i }))` at :23 keeps copies. Test `list with no argument still returns every item, as copies` mutates a listed item and re-reads 'a'.
- PASS 3. list({priority:'urgent'}) throws, naming the valid priorities — src/todo.mjs:18-20 throws `priority must be one of ${PRIORITIES.join("|")}` — byte-identical message to add() at :12. Test asserts /priority must be one of low\|normal\|high/.
- PASS 4. npm test is green — Re-ran locally: `ℹ tests 13 / ℹ pass 13 / ℹ fail 0`.
- PASS MUST NOT: change the shape of an item — Diff touches only list(); item literal `{ id, title, done, priority }` at :13 untouched.
- PASS MUST NOT: change add(), complete() or remove() — git diff hunk spans lines 14-24 only, replacing list(); add/complete/remove unchanged.
- PASS Test written first — 5a3b466 test(T2.1): failing tests precedes 2d62ac9 feat(T2.1): filter list by priority.

## Blocking
- none

## Advisory
- **LOW** src/todo.mjs:19 — Validation error message duplicated instead of shared with add()
  - evidence: throw new Error(`priority must be one of ${PRIORITIES.join("|")}`);  // identical literal also at line 12
  - fix: Hoist to one helper/constant, e.g. `const assertPriority = p => { if (!PRIORITIES.includes(p)) throw new Error(...) }`, and call from both add() and list().
