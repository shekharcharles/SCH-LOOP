# Review T1.2 — round 1

**Verdict:** APPROVE  ·  **Spec:** PASS  ·  raw 0 → unique 0, blocking 0, refuted 0

## Spec checks
- PASS AC1: a test asserts remove() twice throws the second time — tests/todo.test.mjs:58-63 `test("remove on an already-removed id throws", ...)` calls `s.remove(a.id)` then `assert.throws(() => s.remove(a.id), new RegExp(`no item ${a.id}`))`. Non-vacuous: src/todo.mjs:27 `if (!items.delete(id)) throw new Error(`no item ${id}`)` produces exactly that message on the second call.
- PASS AC2: a test asserts mutating a listed item does not change the store — tests/todo.test.mjs:65-74 mutates `listed.title = "tampered"` and `listed.done = true`, then re-reads via `const [fresh] = s.list()` and asserts `fresh.title === "x"`, `fresh.done === false`. Non-vacuous: src/todo.mjs:18 `[...items.values()].map(i => ({ ...i }))` is what makes it hold; a reference-returning list() would fail both assertions.
- PASS MUST NOT: no file under src/ is modified — `git show --stat HEAD` = `sdlc/lab/tests/todo.test.mjs | 18 ++++++++++++++++++`, 1 file changed, 18 insertions(+), 0 deletions. Only the test file is touched.
- PASS MUST NOT: no existing test deleted or weakened — Patch is pure append at the end of file (hunk header `@@ -54,3 +54,21 @@`, all lines `+`). Tests at lines 1-56 (8 tests) are byte-identical to the pre-image; zero `-` lines in the diff.
- PASS Ticket scope: tests only, does not touch src/ — Single-file diff under tests/; deterministic checks reported scope-containment ok, diff-matches-claims ok, test ok.

## Blocking
- none

## Advisory
- none
