# Review T2.1a — round 1

**Verdict:** APPROVE  ·  **Spec:** PASS  ·  raw 0 → unique 0, blocking 0, refuted 0

## Spec checks
- PASS 1. a test mutates an item from a filtered list and asserts the store is unchanged — tests/todo.test.mjs:100 — `const filtered = s.list({ priority: "high" })[0]; filtered.title = "tampered"; filtered.done = true;` then `const [fresh] = s.list({ priority: "high" }); assert.equal(fresh.title, "keep me"); assert.equal(fresh.done, false);` Mutation is on a filtered read; assertions are on a fresh filtered re-read.
- PASS 2. the test would fail if the filtered branch returned live references — Mutation-verified independently in a scratch copy: removing the copy step in src/todo.mjs list() (`.map(i => ({ ...i }))` deleted, leaving `[...items.values()].filter(...)`) makes the new test fail: `actual: 'tampered', expected: 'keep me'`. Copy safety for the filtered path is carried solely by that `.map`, so the test is load-bearing.
- PASS 3. npm test is green — Ran `npm test` in the worktree: `tests 14 / pass 14 / fail 0`, including `✔ filtered list returns copies: mutating a filtered item does not change the store`.
- PASS MUST NOT change src/todo.mjs — `git show --stat ffc6230` touches only `sdlc/lab/tests/todo.test.mjs`; src/todo.mjs absent from the commit.
- PASS MUST NOT change any existing test — `1 file changed, 11 insertions(+)` — zero deletions; diff is a pure append after line 96, existing 13 tests byte-identical.

## Blocking
- none

## Advisory
- none
